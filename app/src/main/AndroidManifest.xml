<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kciter.famuosfood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_activity_login"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.restuarent.RestaurantListActivity"
            android:label="@string/title_activity_restaurant_list" />
        <activity
            android:name=".ui.auth.SignupActivity"
            android:label="@string/title_activity_signup" />
        <activity android:name=".ui.restuarent.RestaurantDetailActivity" />
        <activity
            android:name=".ui.user.UserInfoActivity"
            android:label="@string/title_activity_user_info" />
        <activity
            android:name=".ui.user.UserEditActivity"
            android:label="@string/title_activity_user_edit" />
        <activity android:name=".ui.restuarent.RestaurantRegisterActivity"
            android:label="@string/title_activity_restaurant_register" />
    </application>

</manifest>